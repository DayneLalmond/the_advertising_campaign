{{> nav}}
<section class="grainy-gradient-intro">
    <svg height="0" width="0">
        <filter id='f'>
            <feTurbulence type='fractalNoise' baseFrequency='.5' />
        </filter>
    </svg>
    <div class="d-flex justify-content-center align-items-center text-center" style="height: calc(100vh - 26px);">
        <h1 class="display-4 fw-bold text-uppercase text-dark mb-0" style="letter-spacing: 15px;">Group.4
        </h1> 
        <p>We are the Group 4 student who created this full-stack application.</p>

    </div>
</section> 